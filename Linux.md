
### Посмотреть свой ip
```
hostname -I
```

### Установить curl
```
sudo apt update
sudo apt install curl
```
или 
```
apt update
apt install curl

```

### Установить nano
```
sudo apt update
sudo apt install nano
```
или 
```
apt update
apt install nano
```
