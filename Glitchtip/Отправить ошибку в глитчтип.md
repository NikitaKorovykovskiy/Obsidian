Без остановки кода
```
from sentry_sdk import capture_exception

try:
	return func(*args, **kwargs)
except (
	BadRequestError,
	NotFoundError,
	ConnectionTimeout,
	Exception,
) as exc:
	capture_exception(exc) -< Вот оно
	return None
```
